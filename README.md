## Activate Conda Env
`conda activate {full path of ci-proj folder}` or create a new ENV

## Install Packages
Install the required packages from `requirements.txt`

## Run Application
`streamlit run app\app.py`